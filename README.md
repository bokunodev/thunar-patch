# thunar-patch
patch for thunar to disable root warning.


clone https://github.com/xfce-mirror/thunar, download `thunar-window.patch` and `patch.sh` and put it inside thunar directory and run `./patch.sh`.
